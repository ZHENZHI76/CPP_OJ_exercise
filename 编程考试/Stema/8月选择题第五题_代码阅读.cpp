#include <iostream>
using namespace std;

int main(){
    int n=7, m = 1;
    for (int i= 1; i <= n; i++)
    {
        for (int j=1; j<i; j++)
        {
            cout << '-';
        }
        for(int j=i; j <=n; j++)
        {
            if(i+j != n+1)
                cout << '0';
            else
                cout << '-';
            m++;
        }
        cout << endl;
    }
}
/*
这个输出是由你之前提供的代码生成的。这段代码的目的是打印一个特定的模式。这是一个嵌套循环，外层循环控制行数，内层循环控制每行的内容。

这段代码的工作原理如下：

外层循环控制行数，从1到n（在这个例子中，n是7）。

第一个内层循环在每一行的开始打印一些'-'。它打印的'-'的数量等于当前的行数（即i）减1。

第二个内层循环打印剩余的部分。如果当前的行数（i）和列数（j）的和不等于n+1，它就打印'0'，否则打印'-'。

在每一行的结束，它打印一个换行符。

这个模式是由'-'和'0'组成的，其中'-'的数量在每一行逐渐增加，而'0'的数量在每一行逐渐减少。

在每一行中，第一个'-'的位置是由行数决定的（例如，在第一行，没有'-'，在第二行，第一个'-'在第一个位置，等等）。在每一行中，最后一个'-'的位置是由n+1减去行数决定的（例如，在第一行，最后一个'-'在第七个位置，在第二行，最后一个'-'在第六个位置，等等）。


*/